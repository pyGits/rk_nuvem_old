unit uDmCaixa;

interface

uses
  System.SysUtils, System.Classes, Data.DB, MemDS, DBAccess, Uni,Conexao;

type
  TdmCaixa = class(TDataModule)
    qrCarregarCaixa: TUniQuery;
  private
    { Private declarations }
  public
    function retornaIPCaixas(oCaixaList:TStringList):Boolean;
  end;

var
  dmCaixa: TdmCaixa;

implementation

{%CLASSGROUP 'Vcl.Controls.TControl'}

{$R *.dfm}

{ TdmCaixa }

function TdmCaixa.retornaIPCaixas(oCaixaList: TStringList): Boolean;
begin
  oCaixaList := TStringList.Create;
  with qrCaixas do
  begin
    Close;
    SQL.Clear;
    SQL.Add('SELECT * FROM CAIXA');
    try
    Open;

      while not qrCaixas.Eof do
      begin
        oCaixaList.Add(qrCaixas.FieldByName('IP').AsString);
      end;

    except
    on E:Exception do
    begin
      raise Exception.Create('Erro ao carregar lista de caixas');
    end;

    end;
  end;
end;

end.
